# git hooks：pnpm install 时由 lefthook 的 postinstall 自动安装

pre-push:
  parallel: true
  jobs:
    - name: format
      run: pnpm format:check
    - name: knip
      run: pnpm knip
    - name: typecheck
      run: pnpm typecheck
    - name: test
      run: pnpm test
